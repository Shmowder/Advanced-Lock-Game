This repository represents part of VHDL project (advanced lock game) and made to help track all changes of the .qsf file, which contains project-specific settings, pin assignments and compilation constraints. 
The name of the file correlates with the project name in Quartus, therefore no version name is included in it.
The cangelog for the file is available where the most recent changes correspond to the available .qsf file.
